<view hidden="{{hidden}}" class="movie-container">
    <image class="img-banner" src="{{img}}"></image>
    <image class="img-title" src="images/essay@tag.png"></image>
    <text class="text">{{content}}</text>
</view>